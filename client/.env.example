# Copy to .env.local for local development
# Vite will inject VITE_* vars at build time

# Local development (Vite proxy handles /api → :5000)
VITE_API_URL=

# Production (set this in Vercel / Render dashboard)
# VITE_API_URL=https://muhsinah-api.onrender.com/api
